import s from './MainPageCollection.module.scss';

const MainPageCollection = () => {
  return (
    <section>
      <h2 className={s.title}>MainPageCollection</h2>
    </section>
  );
};

export default MainPageCollection;
