/**
 * Повертає orderId (наприклад, збережений у базі даних)
 * Поки що повертаємо просте число.
 */
export const getOrderId = async () => {
  // TODO: реалізувати збереження та отримання orderId із бази даних
  return 1;
};
